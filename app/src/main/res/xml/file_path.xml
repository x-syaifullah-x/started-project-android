<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!-- /data/data/package_name/files == Context.getFilesDir()-->
    <files-path
        name="files"
        path="." />

    <!-- /data/user/0/package_name/cache or /data/data/package_name/cache  == Context.getCacheDir() -->
    <cache-path
        name="cache"
        path="." />

    <!-- /storage/emulated/0 == Environment.getExternalStorageDirectory() -->
    <external-path
        name="external"
        path="." />

    <!--  Context#getExternalFilesDir(String) Context.getExternalFilesDir(null) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Context.getExternalCacheDir() -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!--  Context.getExternalMediaDirs() -->
    <external-media-path
        name="external_media"
        path="." />
</paths>